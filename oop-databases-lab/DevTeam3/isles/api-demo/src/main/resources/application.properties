spring.application.name=api-demo

# Server configuration
server.port=8080

# H2 database configuration for development
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# PostgreSQL migration settings
# H2 settings above are commented out for PostgreSQL migration.
spring.datasource.url=jdbc:postgresql://localhost:5432/apidemo
spring.datasource.username=postgres
spring.datasource.password=dmisles2007
spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging
logging.level.com.isles.springboot=DEBUG
